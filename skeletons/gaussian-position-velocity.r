decision_fxn <- function(omega) {
    # Make an estimate for the initial position pos0 and the velocity vel, based on
    #   the measurements in omega
    pos0 <- 0;
    vel <- 0;
    c(pos0, vel)
}
